# Example environment variables for the Music module

# Enable or disable the music module
MODULE_MUSIC_ENABLED=true

# Lavalink Node Configuration (JSON array of objects)
# Each object should have: "id", "host", "port", "password" (or "authorization"), "secure" (optional, default: false)
# Example: [
#   {
#     "id": "main",
#     "host": "localhost",
#     "port": 2333,
#     "password": "youshallnotpass",
#     "secure": false
#   }
# ]
# You can set LAVALINK_NODES to a JSON array string, or use legacy single-node vars below.
LAVALINK_NODES=[]

# Legacy single-node variables (optional if LAVALINK_NODES provided)
LAVALINK_HOST=localhost
LAVALINK_PORT=2333
LAVALINK_PASSWORD=youshallnotpass
# Set to true to use TLS, or set LAVALINK_USE_TLS=true
LAVALINK_SECURE=false
