# Leveling module environment example
# Optional: enable Canvas-based rank card generation (requires canvas lib installed)
LEVELING_CANVAS_ENABLED=false
# Default values for guild configs when new guild created or missing
LEVELING_DEFAULT_XP_PER_MESSAGE=15
LEVELING_DEFAULT_COOLDOWN_SECONDS=60
LEVELING_DEFAULT_XP_CAP_PER_WINDOW=300
LEVELING_DEFAULT_MIN_MESSAGE_LENGTH=5
LEVELING_DEFAULT_FORMULA_TYPE=linear
LEVELING_DEFAULT_BASE_XP=100
LEVELING_DEFAULT_GROWTH_FACTOR=1.2
# Optional: toggle temporary role background worker interval (seconds)
LEVELING_ROLE_CLEANUP_INTERVAL=3600
