# TempVC Module Environment Variables
# TEMPVC_CATEGORY_ID= # Optional: Category ID where temporary voice channels will be created
# TEMPVC_CHANNEL_NAME_TEMPLATE= # Optional: Template for temporary voice channel names. Use {username} and {discriminator}
