/**
 * Registers all component interaction handlers (buttons, select menus) for WelcomeLeaveModule.
 * All custom IDs are prefixed with 'welcomeleave:'.
 * Returns a disposer for hot-reload safety.
 */
export async function registerComponentHandlers(ctx) {
  // Stub handler removed. All real handlers are registered in setup.js.
  // This function is now a no-op for hot-reload compatibility.
  return () => {};
}